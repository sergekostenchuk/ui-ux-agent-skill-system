interface:
  display_name: "Figma Assets Manager"
  short_description: "Export and prepare Figma assets for UI work"
  default_prompt: "Use $figma-assets-manager to inventory, export, and prepare Figma assets with privacy, naming, format, and implementation constraints."
policy:
  allow_implicit_invocation: true
