interface:
  display_name: "Figma Design System Sync"
  short_description: "Sync Figma variables, components, and code"
  default_prompt: "Use $figma-design-system-sync to audit or sync Figma variables, styles, components, Code Connect, and local design tokens."
policy:
  allow_implicit_invocation: true
