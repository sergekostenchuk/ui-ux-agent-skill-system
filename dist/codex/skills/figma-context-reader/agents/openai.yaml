interface:
  display_name: "Figma Context Reader"
  short_description: "Read Figma frames, nodes, tokens, and components"
  default_prompt: "Use $figma-context-reader to extract a structured, read-only Figma context brief with privacy and evidence labels."
policy:
  allow_implicit_invocation: true
