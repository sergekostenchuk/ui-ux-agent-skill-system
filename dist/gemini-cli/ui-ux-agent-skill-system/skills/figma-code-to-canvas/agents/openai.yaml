interface:
  display_name: "Figma Code To Canvas"
  short_description: "Convert live UI into editable Figma layers"
  default_prompt: "Use $figma-code-to-canvas to sync a local or live UI back into editable Figma canvas layers with privacy and reconciliation evidence."
policy:
  allow_implicit_invocation: true
